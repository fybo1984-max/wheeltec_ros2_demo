# WHEELTEC 大模型语音控制启动方式与注意事项

适用环境：ROS 2 Humble、M2 麦克风音响、`~/wheeltec_ros2` 工作空间。

## 一、启动前检查

1. 给小车留出足够的安全空间，确认急停可用、电量充足，底盘周围没有人和障碍物。
2. 确认麦克风串口存在：

   ```bash
   ls -l /dev/wheeltec_mic
   ```

3. 确认 M2 录音设备存在：

   ```bash
   arecord -l
   ```

   正常情况下应看到 `XFM-DP-V0.0.18` 或 `XFMDPV0018`。

4. 确保能正常联网。AIUI 在线语音识别和当前大模型接口都需要网络及有效的 API 凭证。
5. 导航时使用当前 `WHEELTEC` 地图。小车应放在建图起点；如果实际位置和地图原点不一致，需要先设置正确的初始位姿。

## 二、推荐启动方式：语音控制、记录点位和语音导航

只打开一个终端执行：

```bash
cd ~/wheeltec_ros2
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 launch largemodel largemodel_control.launch.py \
  use_nav:=true \
  vad_threshold:=0.7 \
  fixed_command_mode:=true
```

该指令会同时启动：

- 底盘、雷达、相机等传感器；
- AIUI 麦克风和在线语音识别；
- 大模型指令解析和动作执行；
- Nav2 定位导航；
- 记录点位和语音导航功能。

大模型联合导航会自动使用以下降载参数：

- Nav2 控制频率：10 Hz；
- MPPI 时间步长：0.1 秒；
- MPPI 预测步数：28；
- MPPI 采样数：1000。
- 最小进展距离：0.2 米；
- 进展判定超时：20 秒。

一般不需要手动填写这些参数。如需恢复默认值，可在启动命令后追加：

```bash
nav_controller_frequency:=10.0 nav_model_dt:=0.1 nav_time_steps:=28 nav_batch_size:=1000 \
nav_progress_radius:=0.2 nav_progress_timeout:=20.0
```

## 三、仅进行大模型对话和底盘动作控制

不需要定点导航、记录点位时使用：

```bash
cd ~/wheeltec_ros2
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 launch largemodel largemodel_control.launch.py \
  use_nav:=false \
  vad_threshold:=0.7
```

此模式适合测试“向前移动二十厘米”“左转”“停止”等动作，不启动 Nav2 定点导航。

## 四、建议测试顺序

### 1. 测试语音识别

说：

```text
小微小微
```

听到或看到“请讲/录音已开始，请说话”后，再说：

```text
向前移动二十厘米，然后停止
```

正常日志应包括：

```text
EVENT_CONNECTED_TO_SERVER
EVENT_VAD: BOS
EVENT_VAD: EOS
识别结果(online): 向前移动二十厘米，然后停止
```

### 2. 测试记录点位

小车定位稳定后说：

```text
记录当前位置为门口
```

正常日志应包括：

```text
get_current_pose(门口)
Recorded pose ... '门口'
```

建议使用“门口、充电区、箱子区、工作台”等有明确含义的中文名称，尽量不要只使用 A、B、C 等单字母。

### 3. 测试语音导航

把小车移动到其他位置并保证定位正确，然后说：

```text
前往门口
```

正常情况下应出现 `navigation(...)`，随后 Nav2 开始规划并移动。

当前已经支持按点位名称进行确定性匹配。例如“前往 c 点”会前往名称为 `c` 的记录点，不会误认为系统内置的字母键 `C`。

### 4. 固定口令增强

`fixed_command_mode:=true` 时，A～D、I～K、沙发、桌子、箱子，以及前进、后退、左右转、停止等固定口令会先在本地纠错并直接生成动作，不再依赖大模型解释。未记录的点位只会提示先记录，不会猜测目标。

热词文件位于：

```text
~/wheeltec_ros2/src/wheeltec_mic_aiui/config/AIUI热词_测试命令.txt
```

把该文件内容上传到 APPID 为 `5eb256ca` 的同一个 AIUI 应用的语音识别热词配置中，然后保存、发布并等待配置生效。不能上传到另一个讯飞应用，否则机器人使用的 APPID 读取不到这些热词。

联合导航模式 `use_nav:=true` 已经启动 Nav2，因此“开始导航”只会回复导航已启动；“关闭导航、打开自主建图、关闭自主建图”会被安全拦截，建图和导航模式应在终端中切换，避免两套节点同时控制底盘。

### 5. 不使用麦克风测试控制链路

启动程序后，在第二个终端执行：

```bash
cd ~/wheeltec_ros2
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 topic pub --once /voice_words std_msgs/msg/String \
  "{data: '向前移动二十厘米，然后停止'}"
```

如果该方式能执行而真实语音不能执行，问题通常在唤醒、录音或在线语音识别环节；如果该方式也不能执行，则检查大模型解析、动作服务和底盘节点。

## 五、运行状态检查

在第二个终端执行：

```bash
cd ~/wheeltec_ros2
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 node list
ros2 param get /aiui_node record_device_name
ros2 topic info /voice_words -v
```

M2 当前正确的录音设备参数应为：

```text
hw:CARD=XFMDPV0018,DEV=0
```

需要观察识别文本时运行：

```bash
ros2 topic echo /voice_words
```

## 六、停止方式

在启动终端按一次：

```text
Ctrl+C
```

等待所有节点显示退出后再关闭终端。不要直接断电，也不要在小车运动时直接关闭终端。

如果准备重新启动，应先确认旧节点已经退出：

```bash
ros2 node list
```

## 七、重要注意事项

1. **不要重复启动相同功能。** 使用 `largemodel_control.launch.py use_nav:=true` 时，不要再单独运行以下指令：

   ```bash
   ros2 launch wheeltec_nav2 wheeltec_nav2.launch.py
   ros2 launch wheeltec_mic_aiui mic_start.launch.py
   ros2 launch wheeltec_mic_aiui aiui_chat.launch.py
   ```

   重复启动可能导致串口占用、节点重名、多路 `/cmd_vel` 控制和导航目标互相抢占。
   如果日志出现 `Ignoring unexpected goal response`，通常表示旧的 Nav2 尚未退出或存在多个 `navigate_to_pose` 服务端；先停止所有旧启动终端，再重新启动一套。

2. **当前流程不需要另外启动 Ollama。** `ollama_ros_chat.launch.py` 属于另一套本地 Ollama 对话链路，不要和当前 `largemodel_control.launch.py` 混合启动。

3. **先说唤醒词，再等提示后说完整指令。** 不要把唤醒词和控制指令连续快速说完。建议距离麦克风 0.5～2 米，语速正常、指令简短。

4. **避免连续快速唤醒。** 如果已经显示“录音已开始”，直接说指令即可；再次唤醒会重置本轮录音。

   M2 的拾音方向会随唤醒角度更新。人换到麦克风另一侧后，应重新说一次唤醒词，再等“录音已开始”后说完整口令。

5. **导航前必须确认定位。** 地图、实际环境和初始位姿不一致时，不要发送导航指令。错误定位可能导致小车朝错误方向运动。

6. **语音动作必须明确。** 推荐说“向前移动二十厘米，然后停止”，不要只说“往前走”。没有明确距离的平移动作虽然有限幅保护，但仍应避免模糊指令。

7. **不要多人同时发指令。** 扬声器回声、旁人讲话和环境噪声可能被识别成新命令。

8. **控制循环偶尔超时可以观察，连续超时需要处理。** 偶尔出现一两条 `Control loop missed` 不一定影响导航；如果持续刷屏并伴随小车卡顿，应停止导航并保存完整日志。

9. **不应再出现 `odom` 坐标系不存在。** 如果出现以下错误，应停止并保存日志：

   ```text
   target frame: "odom" ... does not exist
   ```

   当前车辆应使用 `odom_combined`。

10. **点位自动保存。** 语音记录成功后会同时更新运行文件和源码备份：

    ```text
    ~/wheeltec_ros2/install/largemodel/share/largemodel/config/map_mapping.yaml
    ~/wheeltec_ros2/src/largemodel/config/map_mapping.yaml
    ```

    不再需要手动复制。终端出现 `Point mapping automatically backed up` 表示同步成功；如果源码备份失败，音响会明确提示，但当前运行点位仍然有效。

11. **普通启动不需要重新编译。** 只有修改 C++、Python、launch、YAML、`package.xml` 或构建文件后才需要编译。当前相关包的编译指令为：

    ```bash
    cd ~/wheeltec_ros2
    source /opt/ros/humble/setup.bash
    source install/setup.bash

    colcon build --packages-select largemodel wheeltec_mic_aiui wheeltec_nav2
    source install/setup.bash
    ```

12. **不要公开 API Key、APISecret 或 APPID 凭证。** 如果凭证曾出现在聊天、截图或公开日志中，应立即到对应平台重置。

## 八、常见问题判断

| 现象 | 可能原因 | 处理方式 |
|---|---|---|
| `IllegalApiKeyError` | AIUI 应用凭证无效、应用未上线或服务未开通 | 检查 AIUI 应用、服务授权和三项凭证 |
| `Cannot get card index` | M2 声卡名称不正确或 USB 未识别 | 执行 `arecord -l`，检查 `XFMDPV0018` |
| `/voice_words` 没有数据 | 未唤醒、录音未开始或在线识别失败 | 查看 BOS/EOS、网络和 AIUI 连接日志 |
| 发布测试一直等待订阅者 | `model_service` 没启动 | 检查 `ros2 node list` 和启动终端错误 |
| 能对话但小车不动 | 动作服务、底盘节点或 `/cmd_vel` 链路异常 | 先用 `/voice_words` 手动发布测试 |
| 能移动但不能导航 | `use_nav` 未开启、定位不正确或 Nav2 未就绪 | 使用 `use_nav:=true` 并检查地图和初始位姿 |
| 导航目标错误 | 点位名称含糊或定位映射错误 | 使用明确中文点位名并检查 `map_mapping.yaml` |
| BOS/EOS 正常但结果为空 | 讲话音量偏低、距离过远或说话早于录音提示 | 距离调整到 0.5～1.5 米，正对唤醒方向，等“录音已开始”再说 |
| 持续 `Control loop missed` | CPU/GPU 负载过高 | 停止其他视觉程序，使用本文联合导航降载参数 |
| `Ignoring unexpected goal response` | 同时存在多套 Nav2 动作服务端 | 停止旧启动终端，只保留一套联合启动 |

## 九、出现问题时需要保存的信息

请保存并提供：

1. 从启动命令开始到问题发生后的完整终端日志；
2. 实际说出的完整语句；
3. 小车是否有语音回复、是否开始运动、是否中途停止；
4. 以下检查结果：

   ```bash
   ros2 node list
   ros2 topic info /voice_words -v
   ros2 topic info /cmd_vel -v
   ros2 param get /aiui_node record_device_name
   ```
